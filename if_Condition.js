// IF STATEMENT

// The if statement checks whether a condition is true.
//
// If the condition is true, the code inside the block executes.
// If the condition is false, JavaScript skips the block.

let sale = "false"; // String (truthy)

if (sale) {
  console.log("This is first program :: Time to buy!");
}

let sales = false; // Boolean false

if (sales) {
  console.log("This is second program :: Time to buy!");
}